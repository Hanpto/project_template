// PLACEHOLDER — AI-generated code will overwrite this file entirely.
// (INJECTION.json: replace_mode = overwrite)
//
// CONTRACT: The injected code SHOULD define a class `UserCode` with:
//   - static func run(completion: @escaping (Result<Void, Error>) -> Void)  (preferred)
//   - OR any class/struct that performs its logic on init
//
// If no run(completion:) is found, the module is loaded for side-effects only.
//
// This empty placeholder ensures the template compiles even without injection.

import Foundation

class UserCode {
  static func run(completion: @escaping (Result<Void, Error>) -> Void) {
    NSLog("[UserCode] placeholder — no AI code injected yet")
    completion(.success(()))
  }
}
